<h3>Genre Index Page</h3>

<% @genres.each do |genre| %>
    <div><%=link_to(genre.name, genre_path(genre))  %></div>
<% end %>

<h3>Add Genre:</h3>
<div><%=link_to("Click here!", new_genre_path)  %></div>